{
  "homepage": "",
  "jest": {},
  "scripts": {
    "test": "tsc && tslint --project tsconfig.json && jest --verbose --coverage",
    "watch:test": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^20.0.8",
    "coveralls": "^2.13.1",
    "jest": "^21.1.0",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  }
}
